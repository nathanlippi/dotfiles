(load "~/.emacs.d/my-loadpackages.el")
(add-hook 'after-init-hook '(lambda ()
  (load "~/.emacs.d/my-noexternals.el")
))

(load "~/.emacs.d/local-projects.el")
(load "~/.emacs.d/secrets.el" 'noerror)
(load "~/.emacs.d/my-theme.el")
